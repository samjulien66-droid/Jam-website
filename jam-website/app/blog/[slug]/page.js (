import Link from 'next/link'

export default function BlogPostPage({ params }) {
  return (
    <div style={{ paddingTop: '150px', paddingLeft: '50px' }}>
      <h1>Blog Post</h1>
      <p>Slug: {params.slug}</p>
      <Link href="/blog">Back to blog</Link>
    </div>
  )
}
